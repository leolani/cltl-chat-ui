"""Unit tests for cltl-chat-ui.

This file is load-bearing: `make test` runs a bare `python -m unittest`, which
discovers from the project root and silently skips any subdirectory that is not
an importable package. Without it the suite reports "Ran 0 tests ... OK".
"""
